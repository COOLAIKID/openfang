# Lean dependency set for cloud hosting AND the desktop app launcher.
#
# The AI providers (Groq, Gemini, Hugging Face, Mistral, local) all talk over
# plain HTTP via `requests` — no heavy vendor SDKs are needed to run. Keeping
# this list small makes installs fast and reliable on every machine (no native
# builds, no out-of-memory failures). Desktop-only extras (pyautogui, playwright,
# Pillow) are loaded lazily and simply stay dormant when not installed.
fastapi>=0.115.0
uvicorn>=0.32.0
requests>=2.32.0
apscheduler>=3.10.0
beautifulsoup4>=4.12.0
praw>=7.7.0
