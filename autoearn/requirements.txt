groq>=0.11.0
google-generativeai>=0.8.0
huggingface_hub>=0.25.0
ollama>=0.3.0
apscheduler>=3.10.0
fastapi>=0.115.0
uvicorn>=0.32.0
requests>=2.32.0
beautifulsoup4>=4.12.0
praw>=7.7.0
pytest>=8.0.0
# Computer use — desktop automation (requires DISPLAY on Linux)
pyautogui>=0.9.54
Pillow>=10.0.0
# Browser automation (headless, works without display)
playwright>=1.40.0
# Per-agent sandboxes (requires Docker daemon)
# docker is used via the CLI (subprocess), no extra package needed
